# Быстрый старт - Защищенная админка

## Что исправлено

✅ **Аутентификация** - Теперь требуется логин и пароль
✅ **XSS атаки** - Все пользовательские данные экранируются
✅ **Брутфорс** - Защита от подбора пароля (5 попыток → блокировка 15 минут)
✅ **Загрузка файлов** - Проверка типов и расширений файлов
✅ **CORS** - Ограничен доступ только с localhost

## Как запустить

1. **Установить зависимости:**
```bash
cd backend
pip install -r requirements.txt
```

2. **Запустить сервер:**
```bash
python admin.py
```

3. **Открыть админку:**
```
http://localhost:8002/login
```

## Данные для входа

**По умолчанию:**
- **Логин:** `admin`
- **Пароль:** `admin123`

⚠️ **ВАЖНО:** Смените пароль перед использованием в продакшене!

## Как сменить пароль

```python
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
new_password_hash = pwd_context.hash("ваш_новый_пароль")
print(new_password_hash)
```

Затем в файле `admin.py` найдите строку:
```python
ADMIN_PASSWORD_HASH = os.getenv("ADMIN_PASSWORD_HASH", "...")
```

И замените хеш на новый, либо установите переменную окружения:
```bash
export ADMIN_PASSWORD_HASH="$2b$12$ваш_новый_хеш"
```

## Использование API

После логина все запросы к `/api/admin/*` требуют токен:

```bash
# 1. Получить токен
curl -X POST http://localhost:8002/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# Ответ: {"access_token":"eyJ...","token_type":"bearer"}

# 2. Использовать токен
curl http://localhost:8002/api/admin/profiles \
  -H "Authorization: Bearer eyJ..."
```

## Проверка безопасности

### Тест 1: Аутентификация
```bash
# Должна быть ошибка 401
curl http://localhost:8002/api/admin/profiles
```

### Тест 2: Защита от брутфорса
Попробуйте ввести неправильный пароль 6 раз подряд - аккаунт заблокируется на 15 минут.

### Тест 3: XSS защита
Создайте профиль с именем `<script>alert('test')</script>` - скрипт должен отобразиться как текст, а не выполниться.

### Тест 4: Загрузка файлов
Попробуйте загрузить `.exe` или `.php` файл - должна быть ошибка.

## Что дальше?

Для продакшена:
1. Смените пароль администратора
2. Сгенерируйте новый секретный ключ JWT
3. Настройте HTTPS
4. Используйте переменные окружения для секретов
5. Рассмотрите использование Redis для хранения попыток входа

Подробнее см. в файле `SECURITY_FIXES.md`

---
**Все готово! Админка теперь защищена 🔒**
