# 🚀 Telegram Auth Quick Start

Быстрое руководство по использованию Telegram WebApp аутентификации в вашем приложении.

## 🔍 Где доступна аутентификация?

Telegram WebApp аутентификация реализована в **обоих** приложениях:
- **`backend/main`** - Основное пользовательское приложение (порт 8001)
- **`backend/admin.py`** - Административная панель (порт 8002, с интеграцией базы данных)

Оба приложения используют идентичную систему аутентификации с одинаковыми API endpoints.

## ⚡️ Для разработчиков фронтенда

### 1. Подключите Telegram WebApp SDK

```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### 2. Авторизуйте пользователя при запуске

```javascript
const tg = window.Telegram.WebApp;
tg.ready();

// Авторизация
async function auth() {
    const response = await fetch('/api/telegram/auth', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ initData: tg.initData }),
        credentials: 'include'
    });

    const data = await response.json();
    if (data.status === 'success') {
        console.log('Привет,', data.user.first_name);
    }
}

auth();
```

### 3. Используйте защищенные API

```javascript
// Все запросы к API автоматически аутентифицированы через cookies
const response = await fetch('/api/user/files', {
    credentials: 'include'  // Обязательно!
});
```

## 📱 Доступные API Endpoints

### Авторизация
```
POST /api/telegram/auth
Body: { "initData": "..." }
```

### Текущий пользователь
```
GET /api/telegram/me
```

### Выход
```
POST /api/telegram/logout
```

## 🎯 Примеры

### Пример 1: Полная демонстрация
Откройте файл `frontend/telegram-auth-example.html` для интерактивного примера.

### Пример 2: Проверка авторизации
```javascript
async function checkAuth() {
    const res = await fetch('/api/telegram/me', { credentials: 'include' });
    return res.ok;
}
```

## 🔐 Безопасность

✅ **Все уже настроено:**
- HMAC-SHA256 верификация
- Проверка свежести данных (защита от replay attacks)
- Secure cookies (HttpOnly, Secure, SameSite)
- Изоляция данных пользователей

❗️ **Важно:**
- Всегда используйте `credentials: 'include'` в fetch запросах
- Не храните чувствительные данные в localStorage
- В production используйте HTTPS

## 📚 Полная документация

Смотрите [TELEGRAM_WEBAPP_AUTH_GUIDE.md](./TELEGRAM_WEBAPP_AUTH_GUIDE.md) для подробного руководства.

---

**Готово к использованию!** 🎉
