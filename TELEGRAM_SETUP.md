# Telegram Mini App Setup Guide

## Шаг 1: Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите название бота: `Muji Dating`
   - Введите username бота: `muji_dating_bot` (или любой доступный)
4. Сохраните токен бота - он понадобится позже

## Шаг 2: Настройка Web App

1. В чате с @BotFather отправьте `/newapp`
2. Выберите вашего бота
3. Заполните информацию:
   - **Title**: `Muji - Anonymous Dating`
   - **Description**: `100% Anonymous Dating Platform`
   - **Photo**: Загрузите логотип (512x512 px минимум)
   - **Demo GIF** (опционально): Анимация работы приложения
   - **Short name**: `muji` (только латиница, без пробелов)
   - **Web App URL**: URL вашего сайта (например: `https://your-domain.com`)

## Шаг 3: Настройка токена бота на сервере

### Вариант 1: Через переменную окружения (рекомендуется)

```bash
export TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN_FROM_BOTFATHER"
```

Или добавьте в `.env` файл:
```
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_FROM_BOTFATHER
```

### Вариант 2: Прямо в коде

Откройте `backend/admin.py` и замените строку:
```python
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "YOUR_BOT_TOKEN_HERE")
```

На:
```python
TELEGRAM_BOT_TOKEN = "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"  # ваш реальный токен
```

⚠️ **ВАЖНО**: Не коммитьте токен в git! Добавьте в `.gitignore`

## Шаг 4: Генерация иконок для PWA

Используйте один из инструментов:

### PWA Builder (рекомендуется)
1. Перейдите на https://www.pwabuilder.com/imageGenerator
2. Загрузите ваш логотип (1024x1024 px, PNG с прозрачным фоном)
3. Скачайте ZIP с иконками
4. Распакуйте все файлы в папку `frontend/icons/`

### Real Favicon Generator
1. Перейдите на https://realfavicongenerator.net/
2. Загрузите логотип
3. Настройте под платформы (iOS, Android, Windows)
4. Скачайте пакет иконок
5. Скопируйте в `frontend/icons/`

## Шаг 5: Тестирование в Telegram

### Локальное тестирование

1. Запустите ngrok для публичного доступа:
```bash
ngrok http 8002
```

2. Скопируйте HTTPS URL из ngrok (например: `https://abc123.ngrok.io`)

3. Обновите Web App URL в @BotFather:
   - Отправьте `/myapps`
   - Выберите ваш app
   - Нажмите `Edit Web App URL`
   - Вставьте URL из ngrok

4. Откройте вашего бота в Telegram
5. Нажмите на кнопку меню или отправьте команду для запуска Web App

### Продакшн

1. Разверните приложение на сервере с HTTPS
2. Обновите `Web App URL` в @BotFather на ваш продакшн URL
3. Готово!

## Шаг 6: Настройка кнопки запуска в боте

Создайте команду для запуска приложения:

1. В @BotFather отправьте `/setmenubutton`
2. Выберите вашего бота
3. Введите текст кнопки: `Open Muji` или `🔥 Dating`
4. Вставьте short name вашего app: `muji`

Теперь в чате с ботом будет кнопка меню для запуска приложения!

## Шаг 7: Добавление на домашний экран iPhone

Когда пользователь откроет ваш сайт в Safari на iPhone:

1. Через 5 секунд появится баннер "Добавить на домашний экран"
2. Пользователь может нажать "Установить"
3. Для iOS появится инструкция:
   - Нажать кнопку "Поделиться" внизу экрана
   - Выбрать "На экран Домой"
   - Нажать "Добавить"

## Проверка работоспособности

### В браузере (для отладки)
Откройте консоль разработчика (F12):
```javascript
// Проверка Telegram Web App
console.log(window.Telegram?.WebApp);

// Данные пользователя
console.log(window.Telegram?.WebApp?.initDataUnsafe?.user);
```

### В Telegram
1. Откройте бота
2. Запустите Web App
3. В консоли логи должны показать:
   - `Telegram Web App initialized`
   - Данные пользователя

## Возможные проблемы

### 1. "Telegram Web App SDK не загружается"
- Проверьте, что скрипт загружается: `<script src="https://telegram.org/js/telegram-web-app.js"></script>`
- Убедитесь, что открываете в Telegram, а не в браузере

### 2. "initData пустой"
- Это нормально при открытии в обычном браузере
- В Telegram данные будут автоматически

### 3. "Invalid Telegram auth"
- Проверьте правильность токена бота
- В коде временно отключена проверка (закомментирована) для тестирования
- Раскомментируйте после настройки токена

### 4. "Иконки не отображаются"
- Убедитесь, что все иконки созданы
- Проверьте пути в `manifest.json`
- Проверьте, что сервер отдает статические файлы из `/icons/`

## Дополнительные возможности

### Использование данных пользователя Telegram

JavaScript (frontend):
```javascript
if (telegramUser) {
    console.log('User ID:', telegramUser.id);
    console.log('Name:', telegramUser.first_name);
    console.log('Username:', telegramUser.username);
    console.log('Language:', telegramUser.language_code);
}
```

### Адаптация языка

```javascript
// Автоматический выбор языка
if (telegramUser?.language_code) {
    currentLanguage = telegramUser.language_code; // 'ru', 'en', 'es' и т.д.
}
```

### Haptic Feedback

```javascript
// Вибрация при нажатии кнопки
if (tg) {
    tg.HapticFeedback.impactOccurred('medium');
}
```

### Главная кнопка Telegram

```javascript
// Показать кнопку внизу Telegram
tg.MainButton.text = 'Continue';
tg.MainButton.show();
tg.MainButton.onClick(() => {
    console.log('Main button clicked!');
});
```

## Полезные ссылки

- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [PWA Builder](https://www.pwabuilder.com/)
- [Web App Manifest Spec](https://developer.mozilla.org/en-US/docs/Web/Manifest)

## Поддержка

Если что-то не работает:
1. Проверьте консоль браузера на ошибки
2. Проверьте логи сервера
3. Убедитесь, что все файлы на месте
4. Проверьте, что HTTPS настроен правильно (Telegram требует HTTPS)
