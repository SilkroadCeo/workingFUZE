# Настройка Telegram уведомлений

## Описание

Система уведомлений в Telegram позволяет администратору получать мгновенные уведомления о новых сообщениях от пользователей.

## Что вы получите

Когда пользователь отправляет сообщение в чат с профилем, вы получите уведомление в Telegram с информацией:
- 👤 Имя профиля
- 💬 Текст сообщения
- 📎 Информация о прикрепленных файлах
- ⏰ Время отправки

## Настройка

### Шаг 1: Получите ваш Telegram ID

Есть несколько способов узнать свой Telegram ID:

**Способ 1: Через бота @userinfobot**
1. Откройте Telegram
2. Найдите бота `@userinfobot`
3. Запустите его командой `/start`
4. Бот отправит вам ваш ID (например: `123456789`)

**Способ 2: Через бота @getmyid_bot**
1. Найдите бота `@getmyid_bot`
2. Запустите его командой `/start`
3. Скопируйте ваш ID

### Шаг 2: Запустите вашего бота

1. Откройте Telegram
2. Найдите вашего бота (по токену `8589549087:AAHsjfI75L4w5jgHFN-6RYqhT8dO-ffrkd8`)
3. Нажмите `/start` чтобы активировать бота
4. Это важно! Бот может отправлять сообщения только тем, кто его запустил

### Шаг 3: Настройте переменную окружения

Установите переменную окружения `ADMIN_TELEGRAM_IDS` с вашим ID:

**Для Linux/Mac:**
```bash
export ADMIN_TELEGRAM_IDS="123456789"
```

**Для нескольких администраторов (через запятую):**
```bash
export ADMIN_TELEGRAM_IDS="123456789,987654321"
```

**Или добавьте в файл запуска:**
```bash
# В начале скрипта запуска сервера
export ADMIN_TELEGRAM_IDS="ваш_telegram_id"
cd /home/user/fesgr/backend
python admin.py
```

**Для Docker:**
Добавьте в `docker-compose.yml` или `.env` файл:
```yaml
environment:
  - ADMIN_TELEGRAM_IDS=123456789
```

### Шаг 4: Перезапустите сервер

```bash
cd /home/user/fesgr/backend
python admin.py
```

В логах вы должны увидеть:
```
✅ Telegram bot initialized successfully
```

## Проверка работы

1. Отправьте тестовое сообщение от пользователя к любому профилю через веб-интерфейс
2. Вы должны получить уведомление в Telegram

## Формат уведомления

```
🔔 Новое сообщение от пользователя

👤 Профиль: Анна
💬 Сообщение: Привет! Как дела?
📎 Файл: Прикреплен

⏰ Время: 20.11.2025 15:30:45
```

## Troubleshooting

### Уведомления не приходят

1. **Проверьте, что бот запущен:**
   - Найдите вашего бота в Telegram
   - Отправьте `/start`

2. **Проверьте переменную окружения:**
   ```bash
   echo $ADMIN_TELEGRAM_IDS
   ```

3. **Проверьте логи сервера:**
   - Должно быть: `✅ Telegram bot initialized successfully`
   - Не должно быть: `❌ Failed to initialize Telegram bot`

4. **Проверьте токен бота:**
   - Убедитесь, что токен правильный
   - Токен не должен быть отозван в @BotFather

### Ошибка "Forbidden: bot was blocked by the user"

- Это значит, что вы заблокировали бота в Telegram
- Разблокируйте бота и отправьте `/start`

### Ошибка "Bad Request: chat not found"

- Проверьте правильность Telegram ID
- ID должен быть числом, без пробелов и других символов

## Дополнительные возможности

### Отключение уведомлений для тестирования

Временно удалите переменную окружения:
```bash
unset ADMIN_TELEGRAM_IDS
```

Бот будет работать, но уведомления не будут отправляться.

### Логирование

Все события уведомлений логируются:
- `✅ Notification sent to admin {id}` - уведомление отправлено успешно
- `❌ Failed to send notification to admin {id}` - ошибка отправки
- `⚠️ No admin Telegram IDs configured` - не настроены ID администраторов

## Безопасность

- Токен бота хранится в переменной окружения
- Не коммитьте токен в git
- ID администраторов также в переменных окружения
- Бот может отправлять сообщения только указанным администраторам

## Контакты

Если возникли проблемы, проверьте логи сервера для диагностики.
